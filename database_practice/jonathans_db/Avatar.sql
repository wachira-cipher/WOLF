CREATE TABLE contact_activity(
    form_id INT,
    visitor_id INT,
    activity_type VARCHAR(255),
    activity_date VARCHAR(255),
    email_address VARCHAR(255),
    asset_type VARCHAR(255),
    asset_name VARCHAR(255),
    asset_id INT,
    external_id INT,
    email_recipient_id INT,
    deployment_id INT
    );